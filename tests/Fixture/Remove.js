function (id){const input = new FormData;const self = this;self.name = item.name;self.loading = true;self.error = {};axios.delete('/api/items' + '/' + id, input).then(function (response){self.close();Alert.success('Success', 'Item has been removed.');self.load(self.pagee.page);}).catch(function (error){self.error = error.response.data;}).finally(function (){self.loading = false;});}
