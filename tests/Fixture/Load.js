function (page){const self = this;self.loading = true;let data = { p: page };self.pagee.page = page;data.l = 10;const search = new URLSearchParams(data);axios.get('/api/items' + '?' + search.toString()).then(function (response){const result = response.data;if (result.items.length === 0){self.empty = true;}self.pagee.limit = result.limit;self.pagee.pages = result.pages;self.pagee.total = result.total;self.items = result.items;}).catch(function (error){self.loadError = true;}).finally(function (){self.loading = false;})}